import { makeStyles } from "@fluentui/react-components";

export const useGoogleMapsImportStyles = makeStyles({
  inputContainer: {
    minWidth: 0,
    flexBasis: "480px"
  },
  input: {
    width: "100%",
    minWidth: 0
  }
});
