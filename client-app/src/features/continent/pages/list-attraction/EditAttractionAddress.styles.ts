import { makeStyles } from "@fluentui/react-components";

export const useEditAttractionAddressStyles = makeStyles({
  addressField: {
    width: "100%"
  },
  geoLocationField: {
    width: "200px"
  }
});
