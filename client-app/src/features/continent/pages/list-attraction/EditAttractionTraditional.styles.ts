import { makeStyles, tokens } from "@fluentui/react-components";

export const useEditAttractionTraditionalStyles = makeStyles({
  selectedIndicator: {
    color: tokens.colorBrandBackground
  },
  unselectedIndicator: {
    color: tokens.colorNeutralForeground3
  }
});
